version: '3.8'

services:
  devops-tool:
    build:
      context: .
      dockerfile: Dockerfile
    image: devops-tool:latest
    container_name: devops-tool
    stdin_open: true
    tty: true
    entrypoint: ["/app/app/app.sh"]
    command: ["help"]
